# 从 v0.1.0-beta 迁移到 v1.0.0

v1.0.0 保留旧版的问题范围、证据、竞争解释、价值权重、现实约束和主体性要求，但改写了运行方式。它是公开接口的大版本升级，不承诺旧提示词逐字兼容。

## 版本位置

- 旧版：Git 标签 v0.1.0-beta
- 新版：Git 标签 v1.0.0
- 新版唯一规范：[docs/framework.zh-CN.md](docs/framework.zh-CN.md)

## 主要映射

| v0.1.0-beta | v1.0.0 |
|---|---|
| 固定审计序列 | CORE 默认作答，Adaptive 条件增强 |
| 模块命中后倾向展开 | 插件必须声明可观察增量，最终允许 0 插件 |
| 完整框架可直接当 Prompt | 完整规范只用于维护、审计和评测 |
| 审计结构容易进入前台 | 前台只展示结论、依据、边界和行动 |
| 结论强度与置信表达较早出现 | 证据处理后再限制结论强度，不给未校准百分比 |
| 工具与扩展分析依问题复杂度触发 | 工具由证据义务触发，扩展分析由缺口触发 |

## 推荐迁移

1. 停用旧 prompts/cpaf.*.md。
2. 采用新版 [中文运行提示词](prompts/cpaf.zh-CN.md) 或 [English runtime prompt](prompts/cpaf.en.md)。
3. 不要把完整规范默认注入每次对话。
4. 需要框架审计、回归测试或版本维护时，再读取完整规范。
5. 如果做了衍生实现，请固定引用 CPAF 公共版本号和规范版本号，例如 CPAF v1.0.0 / Spec v3.0.1 Final。

## 兼容判断

满足下面条件，可声明“兼容 CPAF v1.0.0”：

- CORE 是默认路径；
- Adaptive 只有在明确缺口时运行；
- 插件可以为 0，并经过增量举证与删卡；
- 工具结果会更新判断；
- 内部控制面默认不展示；
- 继续分析无决策价值时停止；
- 高风险问题保留必要核验与责任边界。

若修改了这些规则，应说明是衍生版本，不应继续使用原版本兼容声明。

